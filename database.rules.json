{
  "rules": {
    ".read": "false",
    ".write": "false",
    "products": {
      ".read": "true",
      ".write": "true",
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid"
      }
    }
  }
}
